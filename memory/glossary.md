# Glossary

| Term | Meaning |
|------|---------|
| MBQ | MightyBananaQuest — the app |
| MightyBananaQuest | Mobile productivity app (React 18 + TypeScript + Vite, Capacitor) |
| MightyBananaQuestDB | The Dexie IndexedDB database name |
| areaColor | Utility fn in src/lib/areaColor.ts — use instead of raw hsl() for themed colours |
| useIsDark | Hook in src/lib/colorMode.tsx — returns isDark boolean for colour theming |
| uncompleteTask | fn in db.ts — reverses XP, resets done flag, removes habit log |
| capWarning | State in TaskDetailScreen that enforces the 3-active-task cap |
